(function () {
    $('.bxslider').bxSlider({
        mode: 'horizontal'
        // Options
        /*
            mode : fade, vertical, horizontal
            speed : 500 (숫자로 적용)
        */
    });
}());